{% extends 'base.html.twig' %}

{% block title %}{{ parent() ~ ' - ' ~ 'page_title.main.register'|trans }}{% endblock %}

{% block content %}
    <h2>{{ 'subtitle.main.register'|trans }}</h2>

    {{ form(register_form, { attr: { novalidate: '' } }) }}
{% endblock %}
