{% extends 'base.html.twig' %}

{% block title %}{{ parent() ~ ' - ' ~ 'page_title.player.register'|trans }}{% endblock %}

{% block content %}
    <h2>{{ 'subtitle.player.register'|trans }}</h2>

    {{ form(register_form, { attr: { novalidate: '' } }) }}
{% endblock %}
